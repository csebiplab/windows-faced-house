"use client";

import UpperNav from "./UpperNav";

const Navbar = () => {
  return (
    <nav className="w-full p-x-320 bg-white shadow-sm">
      <UpperNav />
    </nav>
  );
};

export default Navbar;
